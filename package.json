{
  "name": "Notes-App",
  "private": true,
  "version": "2.0.0",
  "scripts": {
    "phoenix": "rm -rf node_modules package-lock.json && npm install --no-audit --no-fund --no-package-lock",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "sweetalert2": "11.7.2",
    "vite": "4.1.0"
  }
}
